version: '3.8'
services:
  webclient:
    container_name: webclient
    build: ./webclient
    environment:
      NODE_ENV: production
    ports:
        - 80:80
    volumes:
      - ./src:/usr/share/nginx/html
